<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[
    
export CPPFLAGS="$CPPFLAGS -I$AELTRA_INSTALL_PREFIX/include/nss -I$AELTRA_INSTALL_PREFIX/include/nspr \
    -I$AELTRA_INSTALL_PREFIX/include/nss/private -I$AELTRA_INSTALL_PREFIX/include/nspr/private"

cd "$AELTRA_SOURCE_DIR"
bh_autotools_dev_update
autoreconf -vi

cd "$AELTRA_BUILD_DIR"
"$AELTRA_SOURCE_DIR/configure" \
    --prefix="$AELTRA_INSTALL_PREFIX" \
    --build="$AELTRA_HOST_TYPE" \
    --host="$AELTRA_HOST_TYPE"

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make DESTDIR="$AELTRA_INSTALL_DIR" install

    ]]>
    </install>
</rules>
