<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$AELTRA_SOURCE_DIR"
#autoreconf -vfi || true

cd "$AELTRA_BUILD_DIR"

AWK="$AELTRA_INSTALL_PREFIX/bin/awk" \
PERL="$AELTRA_INSTALL_PREFIX/bin/perl" \
"./configure" \
    --prefix="$AELTRA_INSTALL_PREFIX" \
    --build="$AELTRA_HOST_TYPE" \
    --host="$AELTRA_HOST_TYPE" \
    --disable-nls \
    --disable-rpath

touch aclocal.m4
touch Makefile.in

# prevent this from being re-generated
#touch texindex/texindex.awk

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make DESTDIR="$AELTRA_INSTALL_DIR" install

    ]]>
    </install>
</rules>
