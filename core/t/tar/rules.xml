<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$AELTRA_SOURCE_DIR"
autoreconf -vfi

cd "$AELTRA_BUILD_DIR"
$AELTRA_SOURCE_DIR/configure \
    --prefix="$AELTRA_INSTALL_PREFIX" \
    --build="$AELTRA_BUILD_TYPE" \
    --host="$AELTRA_HOST_TYPE" \
    --disable-nls \
    --libexecdir="$AELTRA_INSTALL_PREFIX/lib/tar" \
    --enable-backup-scripts \
    --with-lzma=xz \
    --disable-silent-rules

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make DESTDIR="$AELTRA_INSTALL_DIR" install

mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tar/rmt" "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/sbin/rmt-tar"
mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/sbin/backup" "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/sbin/tar-backup"
mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/sbin/restore" "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/sbin/tar-restore"
mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tar/backup.sh" "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tar/"
mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tar/dump-remind" "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tar/"

    ]]>
    </install>
</rules>
