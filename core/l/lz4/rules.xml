<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

true

    ]]>
    </prepare>

    <build>
    <![CDATA[

export CC="$AELTRA_HOST_TYPE-gcc"

cd "$AELTRA_BUILD_DIR"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

export CC="$AELTRA_HOST_TYPE-gcc"

cd "$AELTRA_BUILD_DIR"
make PREFIX="$AELTRA_INSTALL_PREFIX" \
     DESTDIR="$AELTRA_INSTALL_DIR" \
     LIBDIR="$AELTRA_INSTALL_PREFIX/lib" \
     install

    ]]>
    </install>
</rules>
