<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$AELTRA_SOURCE_DIR"
bh_autotools_dev_update
autoreconf -vfi

cd "$AELTRA_BUILD_DIR"
"$AELTRA_SOURCE_DIR/configure" \
    --prefix="$AELTRA_INSTALL_PREFIX" \
    --build="$AELTRA_HOST_TYPE" \
    --host="$AELTRA_HOST_TYPE" \
    --includedir="$AELTRA_INSTALL_PREFIX/include/tcl8.6" \
    --enable-shared \
    --enable-threads \
    --disable-nls \
    --with-tcl="$AELTRA_INSTALL_PREFIX/lib/tcl8.6" \
    --with-tclinclude="$AELTRA_INSTALL_PREFIX/include/tcl8.6"

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make SONAME=libexpect.so.5.45 -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make DESTDIR="$AELTRA_INSTALL_DIR" install

mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/expect5.45.4/libexpect5.45.4.so" \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/libexpect.so.5.45"
ln -sf libexpect.so.5.45 \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/libexpect.so.5"
ln -sf libexpect.so.5.45 \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/libexpect.so"

install -d -m 0755 "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tcltk/"
mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/"expect* \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/lib/tcltk/"

mv "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/bin/mkpasswd" \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/bin/expect_mkpasswd"

    ]]>
    </install>
</rules>
