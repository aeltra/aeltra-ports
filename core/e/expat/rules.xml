<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

(cd "$AELTRA_SOURCE_DIR/expat" && ./buildconf.sh)

mkdir -p "$AELTRA_BUILD_DIR/expat"

cd "$AELTRA_BUILD_DIR/expat"
$AELTRA_SOURCE_DIR/expat/configure \
    --prefix="$AELTRA_INSTALL_PREFIX" \
    --build="$AELTRA_BUILD_TYPE" \
    --host="$AELTRA_HOST_TYPE"

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR/expat"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR/expat"
make DESTDIR="$AELTRA_INSTALL_DIR" install

install -d -m 0755 "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/share/aclocal"
install -m 0644 "$AELTRA_SOURCE_DIR/expat/conftools/expat.m4" \
    "$AELTRA_INSTALL_DIR/$AELTRA_INSTALL_PREFIX/share/aclocal"

    ]]>
    </install>
</rules>
