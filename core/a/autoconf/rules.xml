<?xml version="1.0" encoding="utf-8"?>
<rules>
    <prepare>
    <![CDATA[

cd "$AELTRA_SOURCE_DIR"
bh_autotools_dev_update

echo "\
ac_cv_path_EGREP='$AELTRA_INSTALL_PREFIX/bin/grep -E'
ac_cv_path_EXPR=$AELTRA_INSTALL_PREFIX/bin/expr
ac_cv_path_GREP=$AELTRA_INSTALL_PREFIX/bin/grep
ac_cv_path_M4=$AELTRA_INSTALL_PREFIX/bin/m4
ac_cv_path_PERL=$AELTRA_INSTALL_PREFIX/bin/perl
ac_cv_path_SED=$AELTRA_INSTALL_PREFIX/bin/sed
ac_cv_path_install='$AELTRA_INSTALL_PREFIX/bin/install -c'
ac_cv_path_mkdir=$AELTRA_INSTALL_PREFIX/bin/mkdir
" > config.site

export CONFIG_SITE="config.site"

./configure \
    --prefix="$AELTRA_INSTALL_PREFIX"

    ]]>
    </prepare>

    <build>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make -j"$AELTRA_PARALLEL_JOBS"

    ]]>
    </build>

    <install>
    <![CDATA[

cd "$AELTRA_BUILD_DIR"
make DESTDIR="$AELTRA_INSTALL_DIR" install

    ]]>
    </install>
</rules>
